[package]
name = "door-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full", "extra-traits", "printing"] }
quote = "1.0"

[dev-dependencies]
doors = { version = "~0", path = "../doors" }
