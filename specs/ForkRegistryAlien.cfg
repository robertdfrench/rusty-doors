\* The crate as it shipped: teardown released the descriptor twice,
\* because door_revoke closes it and close() closed it again.
CONSTANTS
    Threads = {t1, t2}
    FDs     = {3, 4}
    MaxGen  = 3
    BUGGY   = TRUE

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoAlienClose

\* The state space is bounded by MaxGen on purpose, so running out of
\* moves is expected and says nothing. Only the invariants matter.
CHECK_DEADLOCK FALSE
