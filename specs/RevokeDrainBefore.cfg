\* Question (c). The counter is raised BEFORE the cookie is resolved,
\* with the drain as the only protection, and the kernel free to
\* dispatch a call before revoke ever runs.
CONSTANTS
    Threads   = {t1, t2}
    NumStates = 2
    INCREMENT = "before"
    ARCCLONE  = FALSE
    ORDER     = "drainThenUninstall"

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoUseAfterFree
INVARIANT RevokeGetsOwned
INVARIANT NoCrossTenant

CHECK_DEADLOCK FALSE
