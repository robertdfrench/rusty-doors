\* The same defect, asked only about the worst outcome: closing a
\* descriptor number that another thread has since been handed.
CONSTANTS
    Threads = {t1, t2}
    FDs     = {3, 4}
    MaxGen  = 2
    BUGGY   = TRUE

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoAlienRelease

\* The state space is bounded by MaxGen on purpose, so running out of
\* moves is expected and says nothing. Only the invariants matter.
CHECK_DEADLOCK FALSE
