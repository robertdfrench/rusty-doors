\* Question (b). The counter is raised AFTER the cookie is resolved,
\* with the drain as the only protection. Does the window between those
\* two instructions matter?
CONSTANTS
    Threads   = {t1, t2}
    NumStates = 2
    INCREMENT = "after"
    ARCCLONE  = FALSE
    ORDER     = "drainThenUninstall"

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoUseAfterFree
INVARIANT RevokeGetsOwned
INVARIANT NoCrossTenant

CHECK_DEADLOCK FALSE
